{
  "scripts": {
    "build": "cd repo && yarn && yarn build",
    "copy": "cp -R repo/build /docs",
    "fetch": "git submodule add --force https://github.com/dubisdev/todoist-rest-client-documentation repo && git submodule update --remote -f",
    "send_gh": "git add . && git commit -m \"Updating docs\" && git push",
    "publicar": "yarn fetch && yarn build && yarn copy && yarn send_gh"

  }
}
